@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="container-fluid px-4 py-4">

    <!-- ================= HEADER ================= -->
    <div class="mb-4">
        <h2 class="fw-bold text-dark">Dashboard Overview</h2>
        @* <p class="text-muted mb-0">Real-time statistics of the Clinic Automation System</p> *@
    </div>

    <!-- ================= STAT CARDS ================= -->
    <div class="row g-4">

        <!-- Patients -->
        @* <div class="col-xxl-3 col-xl-4 col-md-6">
            <div class="card stat-card h-100 border-0 shadow-sm">
                <div class="card-body d-flex justify-content-between align-items-center">
                    <div>
                        <p class="text-muted mb-1 small">TOTAL PATIENTS</p>
                        <h3 class="fw-bold mb-0">@ViewBag.TotalPatients</h3>
                    </div>
                    <div class="stat-icon bg-primary">
                        <i class="fas fa-user-injured"></i>
                    </div>
                </div>
            </div>
        </div> *@

        <!-- Physicians -->
        @* <div class="col-xxl-3 col-xl-4 col-md-6">
            <div class="card stat-card h-100 border-0 shadow-sm">
                <div class="card-body d-flex justify-content-between align-items-center">
                    <div>
                        <p class="text-muted mb-1 small">TOTAL PHYSICIANS</p>
                        <h3 class="fw-bold mb-0">@ViewBag.TotalPhysicians</h3>
                    </div>
                    <div class="stat-icon bg-success">
                        <i class="fas fa-user-md"></i>
                    </div>
                </div>
            </div>
        </div> *@

        <!-- Chemists -->
        @* <div class="col-xxl-3 col-xl-4 col-md-6">
            <div class="card stat-card h-100 border-0 shadow-sm">
                <div class="card-body d-flex justify-content-between align-items-center">
                    <div>
                        <p class="text-muted mb-1 small">TOTAL CHEMISTS</p>
                        <h3 class="fw-bold mb-0">@ViewBag.TotalChemists</h3>
                    </div>
                    <div class="stat-icon bg-warning">
                        <i class="fas fa-capsules"></i>
                    </div>
                </div>
            </div>
        </div> *@

        <!-- Suppliers -->
        @* <div class="col-xxl-3 col-xl-4 col-md-6">
            <div class="card stat-card h-100 border-0 shadow-sm">
                <div class="card-body d-flex justify-content-between align-items-center">
                    <div>
                        <p class="text-muted mb-1 small">TOTAL SUPPLIERS</p>
                        <h3 class="fw-bold mb-0">@ViewBag.TotalSuppliers</h3>
                    </div>
                    <div class="stat-icon bg-info">
                        <i class="fas fa-truck"></i>
                    </div>
                </div>
            </div>
        </div> *@

        <!-- Appointments -->
        @* <div class="col-xxl-3 col-xl-4 col-md-6">
            <div class="card stat-card h-100 border-0 shadow-sm">
                <div class="card-body d-flex justify-content-between align-items-center">
                    <div>
                        <p class="text-muted mb-1 small">TOTAL APPOINTMENTS</p>
                        <h3 class="fw-bold mb-0">@ViewBag.TotalAppointments</h3>
                    </div>
                    <div class="stat-icon bg-secondary">
                        <i class="fas fa-calendar-check"></i>
                    </div>
                </div>
            </div>
        </div> *@

        <!-- Schedules -->
        @* <div class="col-xxl-3 col-xl-4 col-md-6">
            <div class="card stat-card h-100 border-0 shadow-sm">
                <div class="card-body d-flex justify-content-between align-items-center">
                    <div>
                        <p class="text-muted mb-1 small">TOTAL SCHEDULES</p>
                        <h3 class="fw-bold mb-0">@ViewBag.TotalSchedules</h3>
                    </div>
                    <div class="stat-icon bg-dark">
                        <i class="fas fa-clock"></i>
                    </div>
                </div>
            </div>
        </div> *@

        <!-- Pending Patients -->
        @* <div class="col-xxl-3 col-xl-4 col-md-6">
            <div class="card stat-card h-100 border-0 shadow-sm border-start border-4 border-danger">
                <div class="card-body d-flex justify-content-between align-items-center">
                    <div>
                        <p class="text-muted mb-1 small">PENDING PATIENTS</p>
                        <h3 class="fw-bold mb-0">@ViewBag.PendingPatientsCount</h3>
                    </div>
                    <div class="stat-icon bg-danger">
                        <i class="fas fa-user-clock"></i>
                    </div>
                </div>
            </div>
        </div> *@

        <!-- Pending Schedules -->
        @* <div class="col-xxl-3 col-xl-4 col-md-6">
            <div class="card stat-card h-100 border-0 shadow-sm border-start border-4 border-warning">
                <div class="card-body d-flex justify-content-between align-items-center">
                    <div>
                        <p class="text-muted mb-1 small">PENDING SCHEDULES</p>
                        <h3 class="fw-bold mb-0">@ViewBag.PendingSchedulesCount</h3>
                    </div>
                    <div class="stat-icon bg-warning">
                        <i class="fas fa-exclamation-circle"></i>
                    </div>
                </div>
            </div>
        </div> *@

    </div>
</div>
